# TMflow Security Report Generator UI 規格文件

## 版本資訊
- **應用程式版本**: v1.0.2.019
- **UI 框架**: Python tkinter + ttk
- **設計主題**: 深色現代化主題
- **規格版本**: 1.1
- **最後更新**: 2026-02-04
- **狀態**: UI 修正穩定版

## UI 模組化架構

根據實際 UI 呈現，系統分為 **六個主要模組區域**：

### 模組定義
1. **MainWindowModule** - 主視窗框架模組 (視窗外框和基礎佈局)
2. **ProjectSelectionModule** - 專案選擇模組 (左上)
3. **ReportOptionsModule** - 報告選項模組 (左中)
4. **ProgressModule** - 進度顯示模組 (左下)
5. **APIConnectionModule** - API 連接模組 (右上)
6. **LogModule** - 日誌顯示模組 (右下)

## 整體佈局規格

### 視窗規格
- **視窗標題**: "TMflow Security Report Generator v1.0.2.019"
- **預設尺寸**: 900x550 像素
- **最小尺寸**: 800x500 像素
- **可調整**: 支援水平和垂直調整
- **背景色**: #2b2b2b (深灰)
- **比例控制**: 左側 60% | 右側 40% (精確比例)

### 佈局結構
```
┌─────────────────────────────────────────────────────────────────────────┐
│ MainWindowModule: TMflow Security Report Generator v1.0.2.008  [─][□][×]│
├─────────────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────┐ ┌─────────────────────────────┐ │
│ │          左側區域 (60%)              │ │        右側區域 (40%)        │ │
│ │                                   │ │                           │ │
│ │ ┌─ 模組2: Select Projects ──────┐   │ │ ┌─ 模組5: API Connection ─┐   │ │
│ │ │ ProjectSelectionModule       │   │ │ │ APIConnectionModule    │   │ │
│ │ │                             │   │ │ │                       │   │ │
│ │ │                             │   │ │ └─────────────────────────┘   │ │
│ │ │                             │   │ │                           │ │
│ │ │                             │   │ │ ┌─ 模組6: Log ───────────┐   │ │
│ │ │                             │   │ │ │ LogModule             │   │ │
│ │ └─────────────────────────────┘   │ │ │                       │   │ │
│ │                                   │ │ │                       │   │ │
│ │ ┌─ 模組3: Report Options ───────┐   │ │ │                       │   │ │
│ │ │ ReportOptionsModule          │   │ │ │                       │   │ │
│ │ └─────────────────────────────┘   │ │ │                       │   │ │
│ │                                   │ │ │                       │   │ │
│ │ ┌─ 模組4: Progress ─────────────┐   │ │ │                       │   │ │
│ │ │ ProgressModule               │   │ │ │                       │   │ │
│ │ └─────────────────────────────┘   │ │ └─────────────────────────┘   │ │
│ └─────────────────────────────────────┘ └─────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
```

## 模組標題規格

### 標題設計原則
- **圖示 + 文字**: 每個模組標題都包含相關的 emoji 圖示和英文描述
- **視覺識別**: 圖示幫助使用者快速識別模組功能
- **一致性**: 所有模組使用相同的標題格式和樣式

### 標題格式規範
```python
# 標準格式：圖示 + 空格 + 英文描述
ttk.LabelFrame(parent, text="🔗 API Connection", style='Dark.TLabelframe')
```

### 各模組標題定義

| 模組 | 標題文字 | 圖示 | 圖示含義 |
|------|----------|------|----------|
| ProjectSelectionModule | 📋 Select Projects & Versions | 📋 | 剪貼板，表示專案清單 |
| ReportOptionsModule | ⚙️ Report Options | ⚙️ | 齒輪，表示設定選項 |
| ProgressModule | 📊 Progress | 📊 | 長條圖，表示進度統計 |
| APIConnectionModule | 🔗 API Connection | 🔗 | 鏈結，表示 API 連接 |
| LogModule | 📝 Log | 📝 | 記事本，表示日誌記錄 |

### 實作範例
```python
# 模組1: 專案選擇
projects_group = ttk.LabelFrame(parent, text="📋 Select Projects & Versions", 
                              style='Dark.TLabelframe', padding="10")

# 模組2: 報告選項
options_group = ttk.LabelFrame(parent, text="⚙️ Report Options", 
                             style='Dark.TLabelframe', padding="10")

# 模組3: 進度顯示
progress_group = ttk.LabelFrame(parent, text="📊 Progress", 
                              style='Dark.TLabelframe', padding="10")

# 模組4: API 連接
api_group = ttk.LabelFrame(parent, text="🔗 API Connection", 
                         style='Dark.TLabelframe', padding="10")

# 模組5: 日誌顯示
log_group = ttk.LabelFrame(parent, text="📝 Log", 
                         style='Dark.TLabelframe', padding="10")
```

## 模組詳細規格

### 模組0: MainWindowModule (主視窗框架模組)

#### 功能職責
- 定義應用程式主視窗的基本框架和屬性
- 管理視窗的尺寸、標題和基本行為
- 提供主要的佈局容器和分割區域
- 處理視窗關閉事件和應用程式生命週期

#### 視覺規格
- **視窗標題**: "TMflow Security Report Generator v1.0.2.XXX"
- **預設尺寸**: 900x550 像素 (基於 v1.0.2.008 實際尺寸)
- **最小尺寸**: 800x500 像素
- **可調整**: 支援水平和垂直調整
- **背景色**: #2b2b2b (深灰)
- **圖示**: 系統預設 tkinter 圖示

#### 佈局架構
```python
# 主視窗設定
root = tk.Tk()
root.title("TMflow Security Report Generator v1.0.2.XXX")
root.geometry("900x550")
root.resizable(True, True)
root.configure(bg='#2b2b2b')

# 主框架 (15px 內邊距)
main_frame = ttk.Frame(root, style='Dark.TFrame', padding="15")
main_frame.pack(fill=tk.BOTH, expand=True)

# 左右分割佈局 - 使用 place() 方法精確控制比例
# 左側區域 (60% 寬度)
left_frame = ttk.Frame(main_frame, style='Dark.TFrame')
left_frame.place(relx=0, rely=0, relwidth=0.6, relheight=1)

# 右側區域 (40% 寬度)  
right_frame = ttk.Frame(main_frame, style='Dark.TFrame')
right_frame.place(relx=0.6, rely=0, relwidth=0.4, relheight=1)
```

#### 區域分配規格
- **左側區域**: 60% 寬度，包含 ProjectSelectionModule、ReportOptionsModule、ProgressModule
- **右側區域**: 40% 寬度，包含 APIConnectionModule、LogModule
- **區域間距**: 15px 水平間距
- **內邊距**: 主框架 15px 全方向內邊距

#### 響應式行為
```python
# 左側區域 - 精確 60% 寬度
left_frame.place(relx=0, rely=0, relwidth=0.6, relheight=1)

# 右側區域 - 精確 40% 寬度
right_frame.place(relx=0.6, rely=0, relwidth=0.4, relheight=1)

# 垂直空間分配
# 左側：ProjectSelection(70%) + ReportOptions(20%) + Progress(10%)
# 右側：APIConnection(30%) + Log(70%)
```

#### 事件處理
```python
# 視窗關閉事件
root.protocol("WM_DELETE_WINDOW", self.on_closing)

def on_closing(self):
    """應用程式關閉時的處理"""
    if self.is_generating:
        if messagebox.askokcancel("確認關閉", "報告正在生成中，確定要關閉應用程式嗎？"):
            self.generation_cancelled = True
            self.save_config()
            self.root.destroy()
    else:
        self.save_config()
        self.root.destroy()
```

#### 版本管理
- **版本號位置**: 視窗標題中
- **版本格式**: "v1.0.2.XXX"
- **更新方式**: 透過 `setup_window()` 方法中的 `root.title()` 設定
- **版本同步**: 必須與 build 腳本中的資料夾名稱保持一致

#### 相依性管理
- **樣式依賴**: 依賴 `setup_style()` 方法設定的深色主題
- **模組容器**: 為其他 5 個功能模組提供容器框架
- **狀態管理**: 管理應用程式的全域狀態（如 `is_generating`）

### 模組1: ProjectSelectionModule (專案選擇模組)

#### 功能職責
- 顯示可用的專案和版本清單
- 支援多選功能
- 提供專案資料重新整理功能
- 管理版本選擇狀態

#### 視覺規格
- **位置**: 左側區域上方
- **佔比**: 左側區域約 70% 高度
- **容器**: ttk.LabelFrame，標題 "Select Projects & Versions"
- **內邊距**: 10px

#### 元件規格
```python
# 工具列按鈕
refresh_button = ttk.Button(text="🔄 Refresh")
select_all_button = ttk.Button(text="Select All") 
clear_all_button = ttk.Button(text="Clear All")

# 專案樹狀檢視
projects_treeview = ttk.Treeview(
    columns=("Select", "Version", "Project ID", "Version ID"),
    height=10,
    style="Dark.Treeview"
)
```

#### 標題規格
- **標題文字**: "📋 Select Projects & Versions"
- **圖示**: 📋 (剪貼板圖示，表示專案清單)
- **樣式**: ttk.LabelFrame 標準標題樣式

#### 資料結構
```python
# 專案資料模型
project_data = {
    "TMflow": [
        {
            "version": "3.12.1600.0",
            "project_id": "4184883033...",
            "version_id": "433622659...",
            "selected": False
        }
    ]
}
```

### 模組2: ReportOptionsModule (報告選項模組)

#### 功能職責
- 設定報告類型 (Standard/Detailed)
- 設定輸出路徑
- 顯示檔名預覽
- 提供報告生成觸發按鈕

#### 視覺規格
- **位置**: 左側區域中間
- **佔比**: 左側區域約 20% 高度
- **容器**: ttk.LabelFrame，標題 "Report Options"
- **內邊距**: 10px

#### 元件規格
```python
# 報告類型選擇
standard_checkbox = tk.Checkbutton(text="Standard Report", default=True)
detailed_checkbox = tk.Checkbutton(text="Detailed Report", default=True)

# 輸出路徑設定
output_entry = ttk.Entry(width=50)
browse_button = ttk.Button(text="📁")

# 生成按鈕
generate_button = ttk.Button(
    text="Generate Reports",
    style="Generate.TButton"  # 藍色高亮樣式
)

# 檔名預覽
preview_label = ttk.Label(
    text="TMflow_3.12.1600.0_Standard_[時間戳]...",
    foreground="#87ceeb"  # 淺藍色
)
```

#### 標題規格
- **標題文字**: "⚙️ Report Options"
- **圖示**: ⚙️ (齒輪圖示，表示設定選項)
- **樣式**: ttk.LabelFrame 標準標題樣式

### 模組3: ProgressModule (進度顯示模組)

#### 功能職責
- 顯示報告生成進度
- 顯示進度百分比
- 提供視覺化進度回饋

#### 視覺規格
- **位置**: 左側區域底部
- **佔比**: 左側區域約 10% 高度
- **容器**: ttk.LabelFrame，標題 "Progress"
- **內邊距**: 10px

#### 元件規格
```python
# 進度條容器 - 使用固定高度確保文字完整顯示
container = ttk.Frame(progress_group, style='Dark.TFrame', height=35)
container.pack(fill=tk.X)
container.pack_propagate(False)  # 防止容器收縮

# 進度框架 - 垂直居中放置
progress_frame = ttk.Frame(container, style='Dark.TFrame')
progress_frame.place(relx=0, rely=0.5, relwidth=1, anchor='w')

# 進度條
progress_bar = ttk.Progressbar(
    mode="determinate",
    maximum=100,
    style="Dark.Horizontal.TProgressbar"
)

# 進度標籤 - 固定寬度確保完整顯示
progress_label = ttk.Label(
    text="0%",
    font=("Arial", 9),
    width=8,
    style="Dark.TLabel"
)
```

#### 佈局重點
- **固定高度容器**: 使用 35px 高度的容器確保垂直空間充足
- **防止收縮**: `pack_propagate(False)` 防止容器被內容壓縮
- **垂直居中**: 使用 `place()` 方法將進度元件垂直居中
- **標籤寬度**: width=8 確保能容納 "100%" 等較長文字

#### 標題規格
- **標題文字**: "📊 Progress"
- **圖示**: 📊 (長條圖圖示，表示進度統計)
- **樣式**: ttk.LabelFrame 標準標題樣式

### 模組4: APIConnectionModule (API 連接模組)

#### 功能職責
- 管理 API 連接設定
- 顯示連接狀態
- 提供重新連接功能
- 管理 API 憑證

#### 視覺規格
- **位置**: 右側區域上方
- **佔比**: 右側區域約 30% 高度
- **容器**: ttk.LabelFrame，標題 "API Connection"
- **內邊距**: 10px

#### 元件規格
```python
# 連接狀態指示器
status_canvas = tk.Canvas(width=12, height=12)
status_indicator = "綠色圓點"  # 已連接狀態

# 重新連接按鈕
reconnect_button = ttk.Button(text="Reconnect")

# API 設定欄位
api_token_entry = ttk.Entry(show="*", width=30)
subdomain_entry = ttk.Entry(width=30)
organization_entry = ttk.Entry(width=30)
```

#### 狀態定義
- **綠色圓點**: API 連接正常
- **黃色圓點**: 連接中
- **紅色圓點**: 連接失敗

#### 標題規格
- **標題文字**: "🔗 API Connection"
- **圖示**: 🔗 (鏈結圖示，表示 API 連接)
- **樣式**: ttk.LabelFrame 標準標題樣式

### 模組5: LogModule (日誌顯示模組)

#### 功能職責
- 顯示應用程式運行日誌
- 記錄操作歷史
- 提供錯誤和狀態訊息
- 支援自動滾動

#### 視覺規格
- **位置**: 右側區域下方
- **佔比**: 右側區域約 70% 高度
- **容器**: ttk.LabelFrame，標題 "Log"
- **內邊距**: 10px

#### 元件規格
```python
# 日誌文字區域
log_text = scrolledtext.ScrolledText(
    wrap=tk.WORD,
    bg="#404040",
    fg="#ffffff",
    insertbackground="#ffffff"
)
```

#### 日誌格式
```
[時間戳] 訊息內容
[15:21:19] 應用程式已啟動
[15:21:19] 配置已載入
[15:28:17] 連接到 FiniteState
[15:28:19] 已載入保存的專案資料
```

#### 標題規格
- **標題文字**: "📝 Log"
- **圖示**: 📝 (記事本圖示，表示日誌記錄)
- **樣式**: ttk.LabelFrame 標準標題樣式

## 模組間通訊規格

### 事件驅動架構
```python
# 模組間事件定義
class UIEvents:
    PROJECT_SELECTED = "project_selected"
    API_CONNECTED = "api_connected"
    REPORT_GENERATED = "report_generated"
    PROGRESS_UPDATED = "progress_updated"
    LOG_MESSAGE = "log_message"
```

### 資料流向
1. **APIConnectionModule** → **ProjectSelectionModule**: API 連接成功後觸發專案載入
2. **ProjectSelectionModule** → **ReportOptionsModule**: 選擇變更時更新檔名預覽
3. **ReportOptionsModule** → **ProgressModule**: 開始生成時重置進度
4. **ProgressModule** → **LogModule**: 進度更新時記錄日誌
5. **所有模組** → **LogModule**: 操作和錯誤訊息記錄

## 樣式規範

### 深色主題配色
```python
COLORS = {
    "background": "#2b2b2b",      # 主背景
    "widget_bg": "#404040",       # 元件背景
    "text": "#ffffff",            # 主要文字
    "accent": "#0078d4",          # 強調色 (按鈕、進度條)
    "success": "#00ff00",         # 成功狀態
    "warning": "#ffff00",         # 警告狀態
    "error": "#ff0000",           # 錯誤狀態
    "preview": "#87ceeb",         # 預覽文字
    "border": "#505050"           # 邊框色
}
```

### ttk 樣式定義
```python
style.configure("Dark.TFrame", background=COLORS["background"])
style.configure("Dark.TLabel", background=COLORS["background"], foreground=COLORS["text"])
style.configure("Dark.TLabelframe", background=COLORS["background"], foreground=COLORS["text"])
style.configure("Dark.TEntry", fieldbackground=COLORS["widget_bg"], foreground=COLORS["text"])
style.configure("Dark.TButton", background=COLORS["widget_bg"], foreground=COLORS["text"])
style.configure("Generate.TButton", background=COLORS["accent"], foreground=COLORS["text"])
style.configure("Dark.Treeview", background=COLORS["widget_bg"], foreground=COLORS["text"])
style.configure("Dark.Horizontal.TProgressbar", background=COLORS["accent"])
```

## 響應式設計規格

### 視窗調整行為
- **水平調整**: 左右區域比例保持 60:40
- **垂直調整**: ProjectSelectionModule 和 LogModule 優先擴展
- **最小尺寸**: 確保所有元件可見和可用

### 元件自適應
```python
# 左側區域
left_frame.pack(side=tk.LEFT, fill=tk.BOTH, expand=True, padx=(0, 15))

# 右側區域  
right_frame.pack(side=tk.RIGHT, fill=tk.BOTH, expand=True)

# 可擴展模組
projects_group.pack(fill=tk.BOTH, expand=True, pady=(0, 10))
log_group.pack(fill=tk.BOTH, expand=True)
```

## 互動設計規格

### 使用者操作流程
1. **啟動** → 載入配置 → 顯示預設狀態
2. **API 連接** → 檢查連接狀態 → 顯示連接結果
3. **載入專案** → 點擊 Refresh → 顯示專案清單
4. **選擇版本** → 點擊選擇框 → 更新預覽
5. **設定選項** → 選擇報告類型和路徑
6. **生成報告** → 點擊 Generate → 顯示進度 → 完成

### 狀態管理
```python
class UIState:
    def __init__(self):
        self.api_connected = False
        self.projects_loaded = False
        self.selected_versions = []
        self.is_generating = False
        self.current_progress = 0
```

## 錯誤處理規格

### 錯誤顯示策略
- **即時驗證**: 輸入欄位即時驗證
- **對話框提示**: 重要錯誤使用 messagebox
- **日誌記錄**: 所有錯誤記錄到 LogModule
- **狀態指示**: 使用顏色和圖示表示狀態

### 錯誤類型定義
```python
class ErrorTypes:
    API_CONNECTION_ERROR = "API 連接錯誤"
    VALIDATION_ERROR = "輸入驗證錯誤"
    FILE_SYSTEM_ERROR = "檔案系統錯誤"
    REPORT_GENERATION_ERROR = "報告生成錯誤"
```

## 效能規格

### 回應時間要求
- **UI 啟動**: < 3 秒
- **模組載入**: < 1 秒
- **API 調用**: < 10 秒 (含超時處理)
- **狀態更新**: < 100ms

### 記憶體使用
- **基礎 UI**: < 50MB
- **專案資料**: < 10MB
- **日誌緩存**: < 5MB
- **總計**: < 100MB

## 測試規格

### UI 測試檢查清單
- [ ] 所有模組正確載入和顯示
- [ ] 模組間通訊正常
- [ ] 響應式佈局正確調整
- [ ] 深色主題樣式一致
- [ ] 錯誤處理正確顯示
- [ ] 狀態指示器正常工作
- [ ] 進度條正確更新
- [ ] 日誌訊息正確記錄
- [ ] **垂直空間充足**: 所有文字元件完整顯示，不被截斷
- [ ] **最長文字測試**: "100%" 等最長內容正確顯示
- [ ] **最小視窗測試**: 調整到最小尺寸時仍然可用

### 跨平台測試
- **Windows**: 主要測試平台
- **macOS**: 相容性測試
- **Linux**: 基本功能測試

---

## 佈局修正記錄

### v1.0.2.019 - 比例控制修正
**問題**: 主視窗最大化後左右區域比例顯示為 50%/50%，不符合規格要求的 60%/40%

**解決方案**: 
- 移除 PanedWindow 方法（weight 參數無法精確控制比例）
- 採用 `place()` 方法進行精確比例控制
- 使用 `relwidth=0.6` 和 `relwidth=0.4` 確保精確比例
- 使用 `relx=0.6` 確保右側區域從正確位置開始

**結果**: 預設視窗和最大化視窗都正確顯示 60%/40% 比例

### v1.0.2.018 - 進度條文字修正
**問題**: 進度條百分比標籤 "0%" 被垂直擠壓，無法完整顯示

**解決方案**:
- 使用固定高度容器 (35px) 確保垂直空間充足
- 使用 `pack_propagate(False)` 防止容器收縮
- 使用 `place()` 方法將進度元件垂直居中

**結果**: 進度條百分比文字完整顯示，無截斷問題

---

**維護說明**: 此規格文件應與實際 UI 實作保持同步，任何 UI 變更都應更新對應的規格內容。