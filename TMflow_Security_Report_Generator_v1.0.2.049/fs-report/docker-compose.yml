version: '3.8'

services:
  fs-report:
    image: ghcr.io/finite-state/fs-report:latest
    environment:
      - FINITE_STATE_AUTH_TOKEN
      - FINITE_STATE_DOMAIN
    volumes:
      - ./fs-reports:/app/output
      # Uncomment the line below to use custom recipes
      # - ./custom-recipes:/app/recipes
    # Uncomment and modify the command below to customize execution
    # command: ["--start", "2025-01-01", "--end", "2025-01-31"] 